name: LSTM Model CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2
      
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'

    - name: Copy Files
      run: |
        cp run.py /github/workspace/run.py
        cp Dockerfile /github/workspace/Dockerfile
        cp bat_with_temp.csv /github/workspace/bat_with_temp.csv

    - name: Build Docker Image
      run: docker build -t lstm-model /github/workspace

    - name: Run Docker Container
      run: docker run lstm-model
